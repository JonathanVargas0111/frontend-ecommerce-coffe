export type FilterTypes = {
    
        schema: {
            attributes: {
                origin: {
                    enum: string[];
                };
            };
        };
    loading: boolean;
    error: string;
};